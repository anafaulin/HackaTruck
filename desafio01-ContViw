//
//  ContentView.swift
//  desafio01
//
//  Created by Turma02-2 on 15/04/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack{
            HStack{
                Rectangle()
                    .fill(Color.red)
                    .frame(width: 100, height: 100)
                    .padding()
                Spacer()
                Rectangle()
                    .fill(Color.blue)
                    .frame(width: 100, height: 100)
                    .padding()
            }
            Spacer()
            HStack{
                Rectangle()
                    .fill(Color.green)
                    .frame(width: 100, height: 100)
                    .padding()
                Spacer()
                Rectangle()
                    .fill(Color.yellow)
                    .frame(width: 100, height: 100)
                    .padding()
            }
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
